name: SaveEvidence
platforms: [android]
intents:
  - intent_authenticate
  - intent_navigate_to_screen
  - intent_navigate_to_game
  - intent_play_game
  - intent_report
risk_tier: low
side_effects: [evidence_write]
description: |
  Copy current screenshot + page-source to evidence/<run>/<label>/. Used at
  incident points (Constitution IV self-healing) and by intent_report.
