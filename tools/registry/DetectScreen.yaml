name: DetectScreen
platforms: [android]
intents:
  - intent_authenticate
  - intent_navigate_to_screen
  - intent_navigate_to_game
  - intent_load_game_context
  - intent_play_game
  - intent_play_bonus
  - intent_report
risk_tier: low
side_effects: []
description: |
  Element-based screen detection via page-source XML + screen_signatures table.
  Returns the matched screen_name. Read-only.
